# ICLR 2027 论文方案（二）：以持续受约束旋转为主线的触觉臂手协作学习

> 整理日期：2026-09-05  
> 目标主题：Applications to Robotics, Autonomy, Planning  
> 依据：[原论文方案](paper_frame1.md)、[臂手协作技术文档](master-slave1.md)、[触觉增强 RL 技术文档](tactie-enhancedRL.md)，以及作者最新说明“已跑通整个框架并成功完成真机拧阀门”。  
> 本文是研究与写作方案。所有建议实验、目标数值、预期机制均不代表已有实验结果。本次未复核训练代码、checkpoint、真机日志或视频；旧技术文档描述的部署缺口不能据此判定为当前系统仍未完成。

## 1. 先回答两个问题

### 1.1 按照原方案，是否必须验证多种真实触觉灵巧手？

**不必须。即使保留原方案，也没有自动产生“必须多种真机手”的要求。**

原方案第 13 节已把第二种手列为后续扩展，第 19 节也明确限制 morphology-agnostic、任意臂手 plug-and-play 等主张。原方案真正较重的证据负担是：同时证明触觉学习、可部署蒸馏、协调接口、技能保持、适应效率和任务扩展。

“一种方法”不等于“一个能直接控制所有硬件的策略”。同一硬件上的多个任务和条件，可以检验方法的有效性及机制；它不能代替跨硬件迁移实验。

| 论文实际主张 | 一种真机灵巧手能否支撑 | 需要补充什么 |
|---|---|---|
| 所提方法改善该平台上的持续受约束旋转 | 可以，前提是结果可靠 | 强基线、重复试验、任务条件变化、机制分析 |
| 同一种手上适用于多个接触操作任务 | 可以 | 各任务独立定义和结果，说明是否重新训练 |
| 不同摩擦、负载、几何下有鲁棒性 | 可以 | 冻结策略后在预先保留的测试条件中评估 |
| 触觉 latent 是本系统中有效的臂手通信变量 | 可以 | 去掉动作/latent 的接口消融及公平的信息量对照 |
| 换不同灵巧手后，方法仍适用 | 单手不足 | 不同形态的验证；仿真验证只能支持仿真层面的结论 |
| 同一策略可零样本迁移到任意手或触觉传感器 | 不足，多测两种也不能证明“任意” | 明确统一接口、未见形态测试、迁移协议及适用边界 |

截至本次核查，ICLR 2027 CFP 和审稿指南没有规定机器人论文必须使用几种真机手。审稿重点包括问题是否清楚、方法是否合理、证据是否支持主张，以及带来了什么新知识。因此，上述判断是“主张—证据匹配”的研究判断，不是录用保证。[ICLR 2027 CFP](https://iclr.cc/Conferences/2027/CallForPapers)、[Reviewer Guidelines](https://iclr.cc/Conferences/2027/ReviewerGuidelines)。

### 1.2 审稿人会不会认为单手验证支撑不了创新？

**可能质疑外部有效性，但是否伤及核心贡献，取决于贡献是否依赖跨手泛化。**

如果贡献写成“面向持续旋转，利用接触历史与当前手部动作学习有效末端补偿”，单手可以进行直接检验。更值得担心的是：

1. 是否只是增加了机械臂自由度，性能自然提高？
2. 是否收益全部来自触觉奖励，协调结构本身没有作用？
3. 是否只有一个特定阀门、一个初始化和几个成功视频？
4. 是否只能完成短角度旋转，尚未证明重复换指和接触恢复？
5. 是否精心调出的规则控制或同等预训练的联合策略也能做到？
6. 是否与已有触觉臂手旋拧方法实质相近？

当前文档的编码器还具有具体硬件结构：115 个节点、固定五指划分、每指独立且输入维度固定的 MLP，以及 21 维手部动作。它是合理的硬件相关实现，但不能仅凭坐标编码和固定维度 latent 就称为形态无关。

资源有限时，优先补齐单手上的有效对照与条件覆盖；不要通过增加第二种手的一段成功视频来替代这些证据。

## 2. 两种论文思路的比较与选择

### 2.1 两种方向都属于目标主题

该主题是 ICLR 的研究领域，不是降低方法与实验要求的独立“应用赛道”。无需同时贡献 robotics、autonomy 和 planning；学习式机器人控制本身就符合主题。没有规划模块时，也不需要为了主题添加规划术语。[ICLR 2027 CFP](https://iclr.cc/Conferences/2027/CallForPapers)。

| 维度 | 原方案：触觉作为可复用策略接口 | 新方案：持续受约束旋转驱动方法设计 |
|---|---|---|
| 中心问题 | 如何串联特权学习、部署蒸馏和技能保持式动作空间扩展 | 如何在负载与运动范围限制下持续旋转，并可靠完成接触交接 |
| 最有价值的结论 | 一种可复用的学习及协调机制 | 一种有明确物理动机、可解释收益的接触操作学习方法 |
| 主要实验负担 | 多个学习阶段、技能保持、适应效率、迁移/复用 | 强任务基线、触觉与协作消融、持续操作、鲁棒性 |
| 对跨手实验的需求 | 仅当主张涉及跨形态时需要 | 当前主线不需要 |
| 与已有真机成果的对应 | 真机阀门支持整条链的一部分 | 真机阀门可以成为主要证据 |
| 主要风险 | 贡献过多，接口可复用或技能保持没有直接证据 | 写成任务展示，缺少超出具体装置的新知识 |
| 适合保留的技术 | 全链路、接口复用、KL 与技能保持 | 触觉反馈、负载/交接 shaping、条件化协作、部署路径 |
| 当前建议 | 作为技术储备与未来扩展方向 | 作为本次投稿主线 |

**建议采用新方向，但保留原方案的技术深度。**

需要改变的是中心研究问题和证据组织方式。不能只在原有系统前增加一段“阀门很难”的介绍，也不能把“完整系统”本身当作全部创新。

推荐主张：

> 我们研究在受约束接触与有限手部运动范围下的持续旋转操作，并检验一种触觉条件化的臂手协作学习结构，能否通过协调局部换指与末端补偿，提高重复操作的成功率与接触稳定性。

原方案更适合的条件是：已经完成跨任务接口复用、同等预算的快速适应、重新锁臂后的技能保留，以及部署前后接口兼容的完整证据。如果这些不是已有结果，本次不宜将它们全部放在摘要里。

### 2.2 原方案哪些内容应保留、收缩或取消必做要求

| 原方案内容 | 新方案处理 |
|---|---|
| 特权触觉学习与 Student 部署 | 保留，作为学习和部署链路；按实际系统写 |
| 动作与触觉 latent 条件化 follower | 保留，作为最值得检验的协调设计 |
| 分阶段训练 | 保留；是否作为贡献取决于消融结果 |
| KL 联合微调 | 可选；仅在有效且实际使用时强调 |
| 技能保持 | 有保留能力测试才主张；否则改写为“从预训练手部策略初始化” |
| latent 蒸馏 | 不设为无条件必做；可直接基于 Student latent 训练 follower |
| 跨任务快速适应 | 没有复用实验时取消该主张 |
| 至少三个语义任务族 | 改为按研究问题选择任务；不是 ICLR 的硬性数量要求 |
| 第二种灵巧手 | 可选后续验证 |
| 完整机械臂仿真 | 有助于解释 sim-to-real；不能把等效滑台写成全臂控制 |
| 视频 case study | 保留，放在量化结果与机制分析之后 |

## 3. 与已有工作的区别必须先讲清楚

### 3.1 最直接的已有工作

| 工作 | 已有内容 | 对本工作的定位约束 |
|---|---|---|
| DexTouch，RA-L 2024 | UR5e＋Allegro、二值触觉、RL 和真机阀门任务；文中阀门成功阈值为 135° | 不能声称首次用触觉 RL 或臂手系统拧阀门。短角度成功不足以区分 |
| DexScrew，ICRA 2026 | 仿真旋转技能、真实遥操作示范、触觉行为克隆，完成螺母/螺丝刀操作 | 触觉历史、旋转步态、腕部调整和分阶段学习已有近邻 |
| DexMove，ICLR 2026 | 面向非抓取操作的触觉与腕指协同学习 | 说明任务导向方法符合 ICLR，但触觉与腕指协同本身不是空白 |
| VTDexManip，ICLR 2025 | 视觉触觉预训练数据与多任务灵巧操作基准 | 稀疏二值触觉对学习有用已被研究，不能作为唯一创新 |
| AnyRotate，CoRL 2024 | 丰富触觉支持多轴手内旋转和 sim-to-real | 应明确受环境约束且存在阻力的旋转，与自由物体重定向的区别 |

以上概述分别来自：[DexTouch 原文](https://arxiv.org/html/2401.12496v2)、[DexScrew 原文 v2](https://arxiv.org/html/2512.02011v2)及其[发表信息](https://arxiv.org/abs/2512.02011)、[DexMove 官方项目](https://peilin-666.github.io/projects/DexMove/)、[VTDexManip 正式论文页](https://proceedings.iclr.cc/paper_files/paper/2025/hash/e19b6f65791e350347bcff8a3955cb5b-Abstract-Conference.html)、[AnyRotate 原文](https://arxiv.org/abs/2405.07391)。

这些只是与当前定位最相关的文献核查，不能据此宣称已穷尽所有同类方法。PP-Tac 检索结果虽出现在 ICLR 网站，其原文标注为 ICLR 2025 workshop paper，不应当作主会录用先例。[PP-Tac 原文](https://openreview.net/pdf?id=f2Ekjmh94V)。

### 3.2 本文应尝试建立的差异

1. **操作条件**：在预先定义的负载、几何误差和时间跨度下研究持续净旋转，定量分析多次接触交接。
2. **协调结构**：手部当前动作与触觉历史共同作为末端补偿策略的条件；需要证明相对于普通联合控制和简单反馈控制的实际收益。
3. **学习与部署**：如果确实成立，突出仅用仿真训练得到可部署的完整协作系统，无需真实示范或真实策略微调。传感标定、控制器调参、人工初始化仍应如实报告。
4. **机制证据**：解释并检验收益是否来自降低接触丢失、缓解运动范围耗尽和提高接触重建成功率。

这些是候选差异，不是已被确认的创新结论。尤其“操作持续更久”本身不够；要对应不同的失败机制、有效的设计选择和可复现的改善。

不应跨论文直接比较原文成功率：机器人、动作自由度、初始化、物体、负载和成功定义均不同。需要在共同协议下重现可比方法；适配实现应标注 adapted baseline。

## 4. 将阀门难点转化为研究问题

### 4.1 建议研究范围

建议术语：

> Sustained constrained rotational manipulation  
> 持续受约束旋转操作

范围由三个性质界定：

- 物体运动受到环境约束，例如绕固定轴旋转；
- 持续进展需要克服外部阻力，并维持足够有效的接触；
- 手部和末端运动范围有限，操作中需要调整接触或姿态。

阀门是代表性任务。相近对象可提供同一物理问题下的补充证据，但不能未经验证推广到插入、任意装配或全部接触操作。

### 4.2 物理动机

用简化的单轴模型解释任务：

$$
I\ddot{\theta} =
\tau_{\mathrm{drive}}-\tau_{\mathrm{resist}},
\qquad
\tau_{\mathrm{drive}} =
\sum_i \hat{\mathbf e}^{\top}
\big((\mathbf p_i-\mathbf p_{\mathrm{axis}})\times
\mathbf f_i\big).
$$

这里关注的是绕目标轴的有效力矩。接触力的法向、切向分解依赖局部表面几何；不能把所有有效力矩简单归为传感器某一个剪切通道。

在局部库仑摩擦近似下，粘着接触还受

$$
\|\mathbf f_{t,i}\|\leq \mu_i f_{n,i},
\qquad q_{\min}\leq q\leq q_{\max}
$$

等约束。增加夹紧力、增加手指位移或移动手腕，均可能改变可用力矩、滑移风险与下一次接触交接条件。

以上是解释设计动机的简化模型，不是新理论，也不证明策略能保证无滑移或稳定性。

### 4.3 五个可检验的难点

| 难点 | 可能出现的现象 | 本文对应机制 | 需要的证据 |
|---|---|---|---|
| 有效力矩不足 | 接触存在但旋转停滞 | 任务相关接触 shaping 与负载分担 | 负载扫描、净转角、仿真有效轴向力矩 |
| 接触不稳定 | 反转、失去有效接触、不可控滑移 | 结构触觉历史反馈 | 同等观测历史的无触觉/粗触觉对照 |
| 手指行程逐渐耗尽 | 关节饱和、有效力臂下降 | 末端补偿与手指换指 | 饱和率、接触重建、相位对齐轨迹 |
| 交接时失去任务进展 | 放开后抓不回，或长时间停顿 | 动作条件化协作及 handover shaping | 接触交接成功率与恢复时间 |
| 训练与部署信号不一致 | 仿真成功但真机不稳定 | 特权训练、可观测 Student 与随机化 | 完整部署输入审计、真机重复测试 |

应把“不打滑、不丢接触”改成更精确的目标：**限制非期望滑移与整体失去有效接触，同时允许完成换指所需的局部释放与受控滑动。**

换指、重抓取和全手重新抓握也须区分。只看到部分手指接触开关，不等于发生了全手重抓取。正文优先用 contact handover / finger gaiting；确有重新建立抓握事件时再使用 regrasping。

## 5. 中心假设和贡献组织

### 5.1 中心假设

> 在持续受约束旋转中，手部动作与触觉历史包含对下一步接触交接有用的信息。使末端策略显式以这些变量为条件，可能比固定手腕、无通信双策略或普通联合策略更有效地维持任务进展。

“可能”需要由实验转变为结论。某些几何下固定手腕可能已足够，某些条件下普通联合策略也可能表现相同；这些结果应帮助界定方法的适用范围。

### 5.2 建议三个贡献

**贡献一：面向持续旋转的触觉条件化臂手学习方法。**  
给出同周期的条件策略分解，使手部局部操作和末端补偿围绕共同接触状态协作。关键证据是同等训练条件下的协调结构比较。

**贡献二：面向接触交接的触觉学习与部署设计。**  
结合任务相关的接触奖励和可部署触觉历史，使训练中的接触行为能够在实际传感限制下执行。PPO、DAgger、Attention、GRU、域随机化都是已有构件；贡献应体现具体设计为何必要，而不是把每个构件分别列为新算法。

**贡献三：围绕持续操作的系统性实证发现。**  
通过仿真消融、负载与几何变化、真机重复试验和接触交接分析，检验方法在何处有效、因何有效、在哪些条件下失败。

如果最终证据显示结构的主要优势是样本效率，应据此重写贡献；若优势只来自奖励，应把主要创新转向有证据的接触学习设计。不能为了既定故事忽略实验结论。

### 5.3 建议标题

优先、主张较克制：

> **Learning Tactile Arm–Hand Coordination for Sustained Valve Turning**

具备相近操作任务证据后：

> **Learning Tactile Arm–Hand Coordination for Sustained Constrained Rotation**

机制证据充分后可用：

> **Keeping Contact While Turning: Tactile Arm–Hand Coordination for Sustained Rotation**

暂不在标题中使用 universal、morphology-agnostic、skill-preserving、plug-and-play 或首次完成等表述。

## 6. 方法：围绕任务闭环融合两项工作

### 6.1 先写清最终部署系统

令 $h_t^q$ 是本体历史，$h_t^B$ 是部署可获得的结构触觉历史，$o_t^A$ 是末端自身状态：

$$
z_t=E_B(h_t^B), \qquad
a_t^H\sim\pi_H(\cdot\mid h_t^q,z_t),
$$

$$
a_t^A\sim\pi_A(\cdot\mid o_t^A,a_t^H,z_t).
$$

联合策略可写作：

$$
\pi(a_t^H,a_t^A\mid o_t)
=\pi_H(a_t^H\mid h_t^q,z_t)
\pi_A(a_t^A\mid o_t^A,a_t^H,z_t).
$$

确定性部署时使用相应动作均值。该表达描述条件结构，不自动意味着采用联合 PPO 优化。

~~~mermaid
flowchart LR
    Q["手部本体历史"] --> H["手部策略"]
    B["可部署触觉历史"] --> E["触觉编码"]
    E --> H
    E --> A["末端补偿策略"]
    H -->|"本周期手部动作"| A
    O["末端自身状态"] --> A
    H --> C["同步下发臂手命令"]
    A --> C
    C --> P["受约束旋转交互"]
    P --> B
    P --> Q
    P --> O
~~~

图对应待据真机实际接口定稿的部署描述。训练期 Teacher、critic 特权信息和奖励应放到另一幅训练图，用虚线表示；不能流入部署 Actor。

### 6.2 触觉部分如何服务任务

根据技术文档，Teacher 可利用节点三维力、物体特权信息和时空触觉；Student 默认只使用本体历史及接触状态、事件、持续时间、接触斑块迁移等结构信息。

可解释的设计动机：

- 接触事件帮助区分接触建立与释放；
- 时间历史帮助区分稳定接触与正在发生的交接；
- 接触斑块变化可提供局部相对运动线索；
- 训练期力信息和任务相关奖励为有效力矩与负载分配提供监督。

这些结构信号并不唯一确定真实接触力、摩擦或滑移。尤其接触质心迁移既可能来自滚动，也可能来自滑动或传感噪声；没有独立标注时只能称为接触迁移线索。

Teacher 和 Student 编码器并不相同。不能把 Teacher 的逐指 Attention–GRU 图直接标成 Student 实机网络；应按实际部署 checkpoint 写明使用的是 Conv1d、GRU 还是更新后的结构。

### 6.3 触觉奖励如何服务任务

把现有奖励按物理作用组织：

1. 有效轴向力矩与任务进度；
2. 容量相关的多指负载分担；
3. 接触交接与时间连续性；
4. 滑移、过载、无效力、手部与末端控制代价。

正文可以写抽象形式 $r=r_{\mathrm{task}}+r_{\mathrm{coord}}-r_{\mathrm{cost}}$；附录必须给实际执行的完整公式、门控、归一化、课程和开关。

技术文档中的 $H/G/S/E$ 并非简单线性相加，不能用一个便于叙事的简式冒充实现。该奖励属于利用任务和仿真接触信息的 reward shaping，除非另有明确界定，不强调为一般性的 intrinsic motivation 算法。

### 6.4 协作部分如何服务任务

文档版 follower 使用当前手部动作、128 维触觉 latent 和末端自身状态；不是显式 option、子目标或高频控制器，因此宜称为“条件化双策略”或“自回归动作分解”。

需要检验的解释是：当前手部动作提供“正在执行什么”的线索，触觉历史提供“当前接触如何演化”的线索，两者一起帮助选择补偿。

它们不等于显式意图标签或未来接触计划。当前 follower 也没有直接读取手指关节角，不能未经验证就声称它准确知道每个手指的剩余行程。增加“follower 读取手部本体状态”的基线，能检验简单状态共享是否已经足够。

### 6.5 蒸馏与臂手训练顺序：按已完成路线写

| 路线 | 适用情况 | 必须处理的接口问题 |
|---|---|---|
| Teacher 手部技能 → Student → 基于 Student 训练 follower | 容易避免 Teacher/Student latent 替换问题 | follower 要在 Student 与可动末端共同诱导的状态分布上训练、评估 |
| Teacher 臂手协作 → 蒸馏完整部署系统 | 已完成联合 Teacher 且最终蒸馏成功 | 手部与末端动作监督、Student 访问状态、协调条件的分布变化 |
| Teacher follower → 替换 Student → follower 再适应/联合修复 | 尽量复用既有 follower | 直接验证替换性能，处理 latent 语义变化与分布偏移 |

优先报告已经跑通的真实路线。若还需选择，第一条路线能减少“强制 Student latent 等价于 Teacher latent”的要求，但仍需检查手部 Student 在移动末端条件下的分布变化。

**latent 维数相同不意味着语义相同；latent 蒸馏也不是所有路线的必要条件。** 若 follower 自始至终基于固定 Student latent 训练，可以不做 Teacher latent 对齐。若直接替换 Teacher latent，则需要对齐、再训练或其他有实证支持的兼容办法。

### 6.6 训练课程与优化应准确呈现

保留“先训练手部、再训练补偿、可选联合微调”的实际流程，并报告总仿真样本、阶段样本和触发条件。

当前文档描述的是共享团队奖励、独立 PPO ratio 与 GAE、follower 输入 detach。应准确称为分阶段/独立策略优化，不声称等价于使用联合 log probability 的 PPO，也不声称有新的单调改进保证。

不必为了任务导向论文强行改写训练器。若优化算法本身不是贡献，公平的 flat、warm-start joint 与接口消融更优先；如果声称独立 PPO 比联合优化更优，则需要专门的优化对照。

## 7. 实验主线：四个研究问题

| 编号 | 研究问题 | 正文必须出现的证据 |
|---|---|---|
| RQ1 | 所提方法能否可靠完成持续旋转？ | 仿真和真机任务指标、重复试验、强基线 |
| RQ2 | 触觉与末端协作各自起什么作用？ | 触觉 × 末端自由度对照、结构/奖励消融 |
| RQ3 | 为什么使用手部动作和触觉作为协调条件？ | 接口消融、状态共享/简单反馈基线、接触交接分析 |
| RQ4 | 改变负载、几何和传感条件后，结论是否保持？ | 冻结策略测试、训练/测试分布、失败边界 |

技能保持、跨手迁移和快速适应不再各占一个必须完成的 RQ。

## 8. 任务与数据划分

### 8.1 主任务：阀门持续旋转

先把当前已经成功的阀门任务变成固定协议：

- 记录阀门外形、尺寸、旋转轴、安装刚度、角度限位和阻力；
- 明确从预抓取状态开始，还是包括寻找、接近和初始抓握；
- 明确末端策略允许控制的轴、工作空间、速度和力/力矩范围；
- 定义目标净转角、回合长度、停滞条件和人工干预计分方式；
- 在冻结模型前确定训练、验证、保留测试条件。

可考虑 360°、720°或 40 s 内的持续净进展，最终应根据真实装置行程和开发集确定。它们是建议设置，不代表目前已经达到，也不能在看完基线结果后选择最有利的阈值。

若真实阀门只有有限角行程，不能要求它完成多圈；可用允许多圈的旋转夹具研究重复操作，并如实称为阀门形状的受约束旋转装置。未测试流体通断、密封与终端锁紧时，不声称完成工业阀门全流程。

### 8.2 仿真任务组合

推荐结构是“一个做深的主任务＋已有条件允许的相近操作”，无需凑三个无关任务。

| 类型 | 建议用途 | 声明边界 |
|---|---|---|
| 同一阀门、不同尺寸/阻力/初始偏差 | 主任务泛化和鲁棒性 | 不计作多个独立任务 |
| 不同接触形状的受约束旋转件 | 检验接触布局变化 | 若只是替换同一轴上的几何，应称对象/几何变化 |
| 真实建模的旋拧工具或螺母任务 | 可选的第二任务 | 需要对应的轴向运动、啮合/进给或任务约束 |
| 自由物体手内旋转 | 可选诊断对照 | 适合研究外部约束的影响，不能单独支撑受约束操作泛化 |

如果仿真将螺母仅建模为绕固定轴旋转的刚体，应称为旋转代理任务，不能声称仿真中完成了螺纹装配。只有 XY 动作时，也不能把需要主动 Z 进给的完整操作加入已支持任务。

没有额外任务时，可以收窄标题为 valve turning，通过丰富实例、严谨负载扫描和机制验证形成聚焦论文；单任务能否达到 ICLR 所需贡献强度，仍取决于发现的意义和实验力度。

### 8.3 泛化术语必须分开

- 同一个冻结策略，测试未见尺寸/阻力：实例或条件泛化。
- 相同训练分布内的新随机样本：分布内测试，不称 OOD。
- 未见范围或预先保留组合：对应的分布外测试，需给范围。
- 相同方法分别训练不同任务：方法适用性，不是策略零样本迁移。
- 相同手、更换机械臂：臂构型变化，不是跨手泛化。
- 同手做节点 dropout：传感退化鲁棒性，不是跨传感器迁移。

## 9. 优先基线与公平性

### 9.1 主基线

| 基线 | 要排除的替代解释 | 实现要求 |
|---|---|---|
| 训练好的触觉 Hand-only | 手本身已经能完成持续任务 | 同样手部训练预算，明确固定腕的实际控制方式 |
| Arm-only / 固定手指抓姿 | 只靠机械臂转动就能完成 | 同一末端动作子空间、工作空间和执行限制 |
| 可部署 flat joint RL | 额外自由度＋普通学习已经足够 | 同级触觉、本体历史、奖励、随机化与调参预算 |
| Warm-start joint RL | 优势只来自手部预训练 | 相同手部 checkpoint、同等后续预算；必要时用可匹配双头结构 |
| 非条件化双策略 | 双网络容量或训练顺序导致收益 | 尽量保持架构和课程，移除显式动作/latent 条件 |
| 手部策略＋规则末端反馈 | 简单控制已能实现必要补偿 | 使用可部署反馈；阈值与增益只在验证条件调节 |
| 完整方法 | 目标方法 | 报告完整训练和部署设置 |

这里的“非条件化双策略”是专门检验通信设计的对照，不要求生硬套用某个 MARL 框架名称。

DexTouch 式可部署触觉＋asymmetric actor-critic 是值得实现的强参考。即便原始网络或代码需要适配，也可在本文动作空间和环境中进行清楚标注的比较。DexScrew 若需真机示范数据，适合作为不同数据预算的比较，不应将其模拟成一个缺少示范的弱基线。

### 9.2 三条公平性原则

1. **比较协调结构时保持奖励一致。** 不能只给完整方法接触 shaping，再将提升全部归因于结构。
2. **比较感知能力时保持训练监督一致。** “部署无触觉”可保留相同特权训练奖励；如果同时删除触觉奖励，需另列实验说明。
3. **比较效率时计入预训练和蒸馏成本。** 同时报告总成本与给定 checkpoint 后的适应成本，不能只报告 follower 的样本量。

flat baseline 应获得至少同等级的可部署信息，包括合理的本体与触觉时间历史。通过容量调整、统一调参预算和 learning curve 检查其是否被系统性限制。

所有方法使用相同任务指标。不同 shaping 下的 raw return 不能作为性能优劣的主要比较。

### 9.3 真机优先部署哪些对照

优先完整方法、触觉 Hand-only、最强可部署 joint baseline，以及一个决定核心主张的消融，例如无触觉训练版本或无触觉通信版本。

仿真中效果很弱但尚未公平调参的基线，不应直接选来拍摄失败视频。强基线也成功时，比较带载能力、持续时长、净进展、接触丢失和恢复效率。

## 10. 核心消融：证明两部分融合的价值

### 10.1 触觉 × 末端协作

| | 固定末端 | 可动末端 |
|---|---|---|
| 仅本体输入 | P0 | P1 |
| 触觉＋本体输入 | P2 | P3 |

尽量用同一训练流程和相同特权监督，明确该表检验的是执行期触觉与末端自由度。可另外增加规则/flat 版本判断结论是否专属于所提结构。

报告各格结果与不确定性。若要声称“协同增益”，可在选定的物理指标 $J$ 上估计：

$$
\Delta_{\mathrm{interaction}}
=(J_{P3}-J_{P2})-(J_{P1}-J_{P0}).
$$

该交互依赖指标尺度，成功率还有天花板效应；它不自动证明某个内部物理因果机制。没有显著交互也可能有两项独立收益，应如实解释。

### 10.2 follower 接口的 2×2 消融

1. 末端自身状态；
2. 自身状态＋当前手部动作；
3. 自身状态＋触觉 latent；
4. 自身状态＋当前手部动作＋触觉 latent。

各版本重新训练，保持 hand checkpoint、奖励与预算一致。补充“共享手部本体状态”或“原始/粗触觉直接输入”的基线，可检验是否必须使用所提 latent。

### 10.3 区分触觉表征和触觉奖励

采用尽量紧凑的两因素对照：

- 基础触觉表示 / 完整时序表示；
- 无接触协调 shaping / 有接触协调 shaping。

然后对最关键项补充：

- 去时间历史；
- 去 handover；
- 去负载分担；
- 直接训练可部署 actor 与 Teacher–Student 路线。

如果奖励是主要贡献，应增加基本接触数量、接触保持奖励等简单 shaping 比较，证明复杂设计的额外价值。

不必将所有网络宽度和损失开关放进主论文。与中心结论无关的工程消融可放附录或暂不做。

### 10.4 执行期干预是补充证据

可测试 latent 延迟、适度丢帧、阶段性的 follower 置零或回放，以观察策略是否依赖反馈；优先在仿真做可控干预。

这类测试会制造训练分布外输入。性能下降只能支持“策略依赖这条信号”，不能单独证明“该信号不可替代”或“没有该模块的方法学不会”。必须配合重新训练的消融。

## 11. 指标与成功定义

### 11.1 主指标

定义展开后的阀门角度与目标方向 $s\in\{-1,1\}$：

$$
\Delta\theta_{\mathrm{net}} =
s\big(\theta_{\mathrm{unwrap}}(T)-\theta_{\mathrm{unwrap}}(0)\big).
$$

以目标净转角、时间预算和预先规定的终止条件定义成功。避免使用累计绝对转角，因为来回振荡可能获得虚假的高分。

建议报告：

- 任务成功率及区间；
- 固定预算下净转角/归一化进度；
- 首次失去持续操作能力的时间；
- 完成时间，同时给出失败比例；
- 负载—成功率/净转角曲线。

不能只统计成功回合的角速度。提前终止的回合要按统一方式计入进度和时间统计；完成时间涉及未完成回合时，可报告到达曲线或明确的删失处理。

### 11.2 机制指标

| 指标 | 推荐定义 | 解释限制 |
|---|---|---|
| 整体接触丢失 | 所有有效手指接触低于阈值且持续超过预设时长 | 个别手指主动释放不算失败 |
| 接触交接成功率 | 事件后重建有效接触，并在固定窗口恢复正向进展 | 报告事件判据、窗口与每回合事件数 |
| 交接停顿/恢复时间 | 从事件开始到恢复预定进展的时间 | 回合中的多个事件有相关性 |
| 关节饱和比例 | 关节进入预定义归一化极限邻域的时间比例 | 不等同于控制器力矩饱和 |
| 接触迁移 | 传感器坐标中的接触斑块位移 | 不能直接标为真实滑移速度 |
| 滑移 | 仿真接触相对切向速度，或真机独立验证的测量 | 区分受控换指滑动与非期望滑移 |
| 有效轴向力矩 | 仿真接触力与轴向力臂计算，或真机仪器测量 | 电机电流/未标定触觉不是精确真值 |
| 末端补偿 | 位移、速度、边界利用率及其与交接的时序关系 | 相关性不等于因果解释 |

重抓取次数主要作为诊断指标，不宜强制放入所有方法的任务成功判据。若另一种合法方法无需重抓取也能完成同样任务，它仍然成功；可以单独分析需要交接的初始条件。

### 11.3 力矩主张需要独立标定

如果标题或贡献强调“带载/高力矩”，应测量真实阻力或使用标定加载装置，报告单位、测量误差及静摩擦/运动阻力差异。没有标定时先称为不同阻力设置。

“转得更快”不自动意味着“能输出更大力矩”；只有负载扫描或相应测量能支持后者。仿真文档中的 nominal 阻力也不能当作真机已测阻力。

## 12. 真机验证：从演示升级为主要证据

### 12.1 实验条件

在一种臂手平台上，建议覆盖：

1. 名义条件；
2. 预先保留的阻力条件；
3. 未见几何或可校准的轴线/初始位置偏差；
4. 允许时增加短暂扰动或传感延迟条件。

选择任务实际可达的偏差；只有 XY 补偿时，不声称能纠正任意 6D 轴线误差。优先冻结策略进行保留条件测试，不为每个测试物体重新调参数。

### 12.2 重复与不确定性

以下是研究资源建议，不是 ICLR 官方门槛：

- 仿真关键比较以 5 个独立训练 seeds 为目标；资源紧张时至少先完成 3 个，并如实报告区间和限制。
- 真机理想安排：3 个独立训练模型，每模型每条件 10 次。
- 例如 4 方法 × 3 模型 × 3 条件 × 10 次，共 360 次；先评估装置复位和标定成本。
- 资源不足时，可先在名义条件做完整模型重复，再对完整方法与最强基线补保留条件；不要在所有维度上各做一两个视频。

仿真并行环境不是独立训练 seeds。真机同一模型的重复回合估计执行随机性，不能替代训练随机性。多个 seed 的结果可分层 bootstrap；单模型成功率可报告二项区间并明确其条件性。交接事件不能全部当独立样本，应按回合/模型聚类。

实验按条件和模型分块随机化顺序，记录日期与硬件状态，避免磨损、温度或标定漂移总与某个方法绑定。模型选择只使用训练/验证信息；如果只能测试一个模型，明确选择规则，不暗示已验证跨 seed 稳定性。

### 12.3 控制输入与评估传感器分开

外部相机或角度编码器可以仅用于测量转角和分析，它们不必成为策略输入。文中画清数据去向。

若策略或自动停止器实际读取角度、视觉或额外力传感器，应在相应系统边界内报告。人工放置阀门、设置初始抓姿和回合之间复位也应记录；“操作阶段自主”不自动等于“从寻找物体到完成作业的全流程自主”。

## 13. Case study 和视频如何使用

推荐顺序：

> 总体量化结果 → 接触/关节/末端时序分析 → 代表性成功与失败视频。

### 13.1 三类有解释力的案例

- 固定腕：转动开始后，接触布局或关节余量恶化，出现停滞/交接失败。
- 弱反馈或无通信：末端动作与局部换指不同步，导致接触重建延迟。
- 完整方法：在同类初始条件下，出现与交接相配合的末端调整并恢复进展。

这些是待验证的分析假设，不是已知结果。也应展示完整方法的失败，例如阻力过大、补偿到达边界或触觉误检。

### 13.2 图与选例规则

同一时间轴展示：净转角/角速度、五指接触、关节余量、末端位移，以及有可靠测量时的轴向力矩/滑移。

以接触释放、重建等事件对齐多条轨迹，展示统计趋势。代表视频可选性能接近中位数的成功回合与最常见失败类别的回合；保留 trial ID、模型 ID 和所有评估日志。

视频注明倍速、初始化、是否干预、是否连续拍摄。避免只剪最成功方法的片段和最差基线片段。

仅凭时间上“手腕动了，然后成功”还不能证明补偿机制。可在仿真复制事件前状态，比较正常反馈、短时冻结和预设补偿的后续差异，再与独立训练消融共同解释。

## 14. 建议正文结构与图表

ICLR 2027 初投稿正文为不超过 9 页，参考文献不计入；讨论/终稿阶段上限为 10 页。以下按初稿 9 页规划。[Author Guidelines](https://iclr.cc/Conferences/2027/AuthorGuidelines)。

| 部分 | 建议篇幅 | 内容 |
|---|---:|---|
| Introduction | 1.0 页 | 任务矛盾、已有方法缺口、中心假设和贡献 |
| Related Work | 0.6 页 | 触觉旋转、臂手控制、sim-to-real，正面比较近邻 |
| Problem Formulation | 0.6 页 | 任务、观测、动作子空间、持续进展与边界 |
| Method | 2.3 页 | 部署架构、触觉学习、条件协作、实际训练顺序 |
| Experiments | 4.0 页 | 主结果、核心消融、负载/几何测试、机制与真机 |
| Limitations and Conclusion | 0.5 页 | 单平台、任务范围、失败条件及主要发现 |

建议图表：

- Figure 1：真实任务与一次完整接触交接，标出有限手部行程和末端补偿。
- Figure 2：最终部署闭环与训练流程；明确特权信息隔离。
- Table 1：仿真主基线结果和总训练成本。
- Table 2：真机成功率、净进展、试验数和保留条件结果。
- Figure 3：负载/持续时间变化下的表现与学习曲线。
- Figure 4：触觉 × 协作、接口消融和接触交接时序；版面不足时拆入附录。

附录放完整奖励、网络、控制器、标定、所有 seed、条件范围和失败分类。不能把支持主要贡献的唯一证据全部移到附录。

## 15. Introduction 与摘要写作草案

### 15.1 Introduction 的五段逻辑

**第一段：提出具体矛盾。**  
持续旋转需要产生有效轴向力矩，但保持大接触力和固定抓姿会限制手指运动；换指释放运动范围，又可能打断力矩传递。阀门提供一个可测量、可重复的代表场景。

**第二段：说明为什么单一局部技能不够。**  
手部可以形成旋转步态，但在某些负载、偏差和长期操作条件下会失去有效接触。末端移动能够改变整个接触配置，同时也可能干扰手指当前动作。这一瓶颈需由基线和诊断数据支持。

**第三段：提出设计假设。**  
结构触觉历史描述接触演化，当前手部动作描述正在执行的局部控制，使二者成为末端补偿策略的条件，可能改善接触交接。

**第四段：简述学习与部署。**  
用特权仿真训练获得接触技能，经实际采用的 Student/协作训练流程部署到真实系统，明确真实数据需求。

**第五段：概述已验证发现与贡献。**  
只放已有且能在正文找到证据的结果，不写预期数字、不宣称未经验证的跨手能力。

### 15.2 中文摘要模板

> 在受环境约束的旋转操作中，灵巧手需要持续传递有效力矩，并在有限关节运动范围内反复调整接触。局部换指和整体末端移动之间的相互影响，使长时间操作容易出现接触丢失和进展停滞。本文研究一种触觉条件化的臂手协作学习方法，以手部当前动作和接触历史共同指导末端补偿，并通过［实际采用的特权训练与部署流程］获得可部署策略。在［任务与测试条件］中，我们与［主要基线］比较，发现［量化结果］。接触交接与控制轨迹分析进一步表明［有证据支持的机制］，并在［一种真实臂手平台］上验证了［明确范围的自主操作能力］。

### 15.3 英文摘要骨架

> Sustained rotation under environmental constraints requires a dexterous hand to transmit useful torque while repeatedly changing contacts within a limited range of motion. We study tactile-conditioned arm–hand coordination for this setting. A hand policy produces local manipulation actions, while an end-effector policy conditions on those actions and tactile history to learn compensatory motion. Through [the implemented training and deployment procedure], we obtain a policy using [actual deployment observations]. Experiments on [tasks and held-out conditions] show [measured findings] relative to [strong baselines]. Real-robot trials and contact-transition analyses establish [supported capability and mechanism] within [stated scope].

不要在实验前补全为“显著优于所有方法”。完成试验后按证据重写。

## 16. 当前系统事实核对表

作者最新说明优先于旧技术文档的时间快照。下表用于补录论文 Methods，不意味着否认真机已跑通。

| 项目 | 文档/本次对话已知 | 投稿材料需补录 |
|---|---|---|
| 真机阀门成功 | 作者已确认 | 视频、任务条件、净转角/时长、重复次数 |
| 真机平台 | 文档仿真手为 Revo3；真机组合未在本次明确 | 机械臂、手、触觉型号及实际控制自由度 |
| 仿真末端 | 文档版为 XY 物理滑台 | 当前使用 XY/yaw/其他自由度，真机映射 |
| 手部训练与部署 | 文档含 Teacher 和 Student | 真机 checkpoint 类型、实际观测、预处理 |
| follower 接口 | 文档版为动作＋Teacher latent＋末端状态 | 真机使用哪一 latent，如何训练/替换/适应 |
| 真机触觉 | 文档 Student 使用无力幅值结构通道 | 当前是否仍如此，是否新增力或其他信号 |
| 连续重抓取 | 作者提出为任务难点 | 自主发生的事件类型、次数、恢复情况 |
| 人工参与 | 未明确 | 初始抓姿、复位、示范、微调、在线干预 |
| 阻力 | 有仿真参数描述 | 真机测量/加载设置与误差 |
| 方法比较 | 本次未提供统计 | 基线、seed、trial、模型选择规则 |
| 联合微调 | 旧文档记录开关不一致 | 对应实验实际配置与运行记录 |

仿真 2D 补偿映射到完整机械臂，可以支持“真实臂手系统中的任务空间协作”。它不自动支持全臂关节空间 RL、全身控制或任意 6D 动作能力。

## 17. 资源有限时的执行优先级

### P0：直接决定中心结论

1. 固化已经成功的真机模型、部署接口、初始化与成功判据。
2. 建立公平的触觉 Hand-only、最强 joint baseline 和关键通信/感知消融。
3. 检验是否确有持续旋转、多次接触交接以及可量化负载差异。
4. 完成关键仿真多 seed 与真机重复试验。
5. 整理与 DexTouch、DexScrew 的具体差异及共同协议下的比较。

### P1：增加论文说服力

1. 冻结策略后的保留几何/负载/偏差条件。
2. 简单规则补偿与本体状态共享基线。
3. 接触交接时序与可控仿真干预。
4. 已具备实现基础的第二操作任务。

### P2：核心证据稳定后再考虑

1. 第二种灵巧手或触觉布局。
2. 跨任务编码器复用与少样本适应。
3. 全面比较 latent 蒸馏结构。
4. 更广动作空间或新优化器。
5. 无关任务扩展。

按官方当前时间，摘要截止为 2026-09-18 23:59 AoE，全文截止为 2026-09-25 23:59 AoE；分别对应北京时间 9 月 19 日、9 月 26 日 19:59。应给平台提交留出余量。[ICLR 2027 CFP](https://iclr.cc/Conferences/2027/CallForPapers)。

以当前日期估算的工作安排：

| 时间 | 主要产出 |
|---|---|
| 9 月 5—7 日 | 系统事实表、冻结评估协议、近邻文献定位、先导结果 |
| 9 月 8—13 日 | 主基线、关键消融与真机量化同时推进 |
| 9 月 14—18 日 | 检查中心主张是否成立，补保留条件，形成主要图表和摘要 |
| 9 月 19—23 日 | 按最终证据完成正文、附录、视频和复现材料 |
| 9 月 24—25 日 | 核对数据与文字一致性，完成格式与提交检查 |

这是安排建议，不能替代对实际训练耗时和真机试验成本的估计。如果关键对照尚未成立，应缩减主张或继续完善，而不是用叙事掩盖缺口。

## 18. 审稿质疑的预案

| 可能质疑 | 需要拿出的证据/调整 |
|---|---|
| 只有一种手，泛化有限 | 限定单平台与任务范围；给冻结策略的对象/条件测试；不声称跨手 |
| 阀门旋转已有人做过 | 正面引用近邻；比较持续操作条件与协调结构，避免“首次”叙述 |
| 只是 PPO、触觉和分阶段训练的组合 | 给设计假设、简单替代方案、受控消融和可推广的经验发现 |
| 加两个自由度当然会更好 | 同动作空间的 joint/规则反馈基线；比较是否改善接触交接 |
| 奖励塑形才是性能来源 | 同奖励结构比较＋独立奖励消融；按结果重写贡献 |
| 无触觉基线被故意削弱 | 同历史、监督和预算；重新训练消融；区分执行期信号与训练奖励 |
| 只是选了成功视频 | 全部 trial 的统计、选例规则、完整方法失败案例 |
| 高力矩或防滑没有测量 | 提供校准与独立测量，否则缩小为净进展/接触稳定性结论 |
| 仿真仅有 XY，为什么叫臂手？ | 明确任务空间动作与真机执行控制器，不主张全臂学习 |
| 重抓取其实是规则触发 | 报告规则与学习的边界，不称涌现规划或完全学习式重抓取 |
| 二值触觉不足以识别隐藏负载 | 不声称精确识别；报告性能范围、历史作用与失效条件 |

## 19. 最终建议与论文完成标准

推荐本次投稿围绕：

> **一种真实灵巧手上的持续受约束旋转，作为触觉条件化臂手协作学习的代表性研究问题。**

保留现有触觉学习与主从结构，用问题驱动的方式解释它们各自解决的困难。将“通用接口、技能保持、快速扩展”收缩为有证据才加入的结论。

本方案达到可提交的研究闭环，需要：

- 真实部署链路与论文描述一致；
- 主要任务定义和持续操作证据清楚；
- 同预算、同观测等级的强基线；
- 至少能区分触觉、奖励和协调结构作用的关键消融；
- 真机重复统计和条件变化；
- 对已有工作、限制和失败模式的准确陈述。

第二种手是扩展外部有效性的方式之一。当前最有价值的工作，是在已经成功的平台上明确证明：方法改善了什么，改善来自哪里，以及结论能保持到什么范围。
